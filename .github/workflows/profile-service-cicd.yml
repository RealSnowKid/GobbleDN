name: Profile Service CI/CD

defaults:
  run:
    working-directory: backend/profile-service

on:
  push:
    branches: master
  pull_request:
    branches: master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK
      uses: actions/setup-java@v1
      with:
        java-version: 17
    - name: Build with Maven
      run: mvn clean install
    - name: Login to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
    - name: Publish to Docker Hub
      uses: docker/build-push-action@v2
      with:
        context: .
        push: true
        tags: snowkid/profile-service:1
# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#     - uses: actions/checkout@v1
#       with:
#         ref: master
#     - name: Set up JDK 17
#       uses: actions/setup-java@v1
#       with:
#         java-version: 17
#     - name: Build and release
#       run: mvn clean package
#   docker:
#     environment: docker
#     runs-on: ubuntu-latest 
#     needs: build
#     steps:
#       # - name: Set up QEMU
#       #   uses: docker/setup-qemu-action@v1
#       # - name: Set up Docker Buildx
#       #   uses: docker/setup-buildx-action@v1
#       - name: Check out the repo
#         uses: actions/checkout@v3
#       - name: Login to DockerHub
#         uses: docker/login-action@v1
#         with:
#           username: ${{ secrets.DOCKER_USERNAME }}
#           password: ${{ secrets.DOCKER_PASSWORD }}
#       # - name: Build and push Gateway
#       #   uses: docker/build-push-action@v2
#       #   with:
#       #     push: true
#       #     context: "{{defaultContext}}:backend/profile-service"
#       #     tags: snowkid/profile-service:latest
#       - name: Extract metadata (tags, labels) for Docker
#         id: meta
#         uses: docker/metadata-action@v4
#         with:
#           images: snowkid/profile-service
#       - name: Build and push Gateway
#         uses: docker/build-push-action@v2
#         with:
#           context: .
#           push: true
#           tags: ${{ steps.meta.outputs.tags }}
#           labels: ${{ steps.meta.outputs.labels }}